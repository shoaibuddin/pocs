module.exports = function (PageConstructor) {
    var page = new PageConstructor();

    browser.driver.get(wwwHost + page.uri);
    browser.driver.manage().window().maximize();
    browser.sleep(3);
    browser.waitForAngular();

    return page;
};