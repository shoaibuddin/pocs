var carsConfig = require("./cars-config.json");

global.wwwHost = carsConfig.properties.windshield.www.host;
global.to = require("./e2e/util/to");
global.at = require("./e2e/util/at");
global.toMobile = require("./e2e/util/toMobile");

exports.config = {
    directConnect: true,
    specs: ['./e2e/**/*.test.js'],
    capabilities: {
        //'browserName': 'firefox'
        'browserName': 'chrome'
    },
    jasmineNodeOpts: {
        print: function() {}
    },
    onPrepare: function () {
        var SpecReporter = require('jasmine-spec-reporter');
        jasmine.getEnv().addReporter(new SpecReporter({
            displayStacktrace: 'all'
        }));
    }
};